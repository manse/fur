html,
body {
  margin: 0;
  padding: 0;
  width: 100%;
  height: 100%;
  overflow: hidden;
}

:global #app {
  width: 100%;
  height: 100%;
}

.display-switch {
  position: absolute;
  bottom: 25px;
  left: 50%;
  transform: translate(-50%, 0);
}
